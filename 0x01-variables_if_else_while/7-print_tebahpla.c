#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0; (success)
 */

int main(void)
{
	char  cs;

	cs = 'z';
		while
			(cs >= 'a') {
				putchar(cs);
				cs--;
			}
	putchar('\n');
	return (0);
}

