#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main -Print alphabet in lower case,follwed by new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');

	return (0);
}

